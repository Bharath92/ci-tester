language: java

matrix:
  include:
    - jdk: openjdk8
      env: ARCH=x86_64
    - jdk: openjdk8
      env: ARCH=aarch64
      nodePool: u16
    - jdk: openjdk8
      env: ARCH=aarch32
      nodePool: u14

build:
  ci:
    - echo hi
