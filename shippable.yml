language: none

runtime:
  nodePool: u14

env:
  - foo=bar
  - doo=dar

build:
  ci:
    - echo hi
    - |
      if [ "$foo" == "bar" ]; then
        afsd
      fi

matrix:
  allow_failure:
    - nodePool: u14
      env: foo=bar
