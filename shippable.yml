language: python

#matrix:
#  include:
#    - env: foo=bar

env:
  - foo=bar
  - boo=far

build:
  ci:
    - docker info
    - docker ps -a
    - docker images
    - |
      lol() {
        echo lol
      }
      shippable_retry lol
    #- |
    #  [ $(docker version --format {{.Server.Version}}) == "17.06.0-ce" ]
