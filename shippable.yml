language: java

java:
  - openjdk8

envs:
  - foo=bar
  - foo=var

matrix:
  allow_failures:
    - jdk: openjdk8
      env: foo=var

build:
  ci:
    - echo hii
    - echo $foo
    - |
      if [ "$foo" == "var" ]; then
        adaddf
      fi
