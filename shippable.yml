language: java

jdk: openjdk8

runtime:
  nodePool: centos7

matrix:
  include:
    - jdk: openjdk8
      env: ARCH=x86_64
    - jdk: openjdk8
      env: ARCH=aarch64
      nodePool: u16

build:
  ci:
    - echo hi
