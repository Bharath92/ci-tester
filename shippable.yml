language: python

matrix:
  include:
    env: foo=bar

build:
  ci:
    - docker info
    - docker ps -a
    - docker images
    #- |
    #  [ $(docker version --format {{.Server.Version}}) == "17.06.0-ce" ]
