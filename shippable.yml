language: node_js

node_js:
  - 8.9.3

#runtime:
#  nodePool: u16
#
#python:
#  - 3.7
#  - 2.7
#
#matrix:
#  include:
#    - env: foo=bar
#      nodePool: u14
#    - env: boo=far


build:
  ci:
    - npm install gulp@4.0.0 -g
    - |
      if ! npm install -ddd; then
        pushd /root/.npm/_logs
        cat $(ls -At | head -n1)
        popd
        exit 1
      fi
    
