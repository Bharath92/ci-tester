language: node_js

node_js:
  - 8.9.3

#runtime:
#  nodePool: u16
#
#python:
#  - 3.7
#  - 2.7
#
#matrix:
#  include:
#    - env: foo=bar
#      nodePool: u14
#    - env: boo=far


build:
  ci:
    - npm install
    - npm cache verify
    - npm cache clean --force
    - npm cache verify
    
