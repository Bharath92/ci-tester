language: none

runtime:
  - nodePool: u14
  - nodePool: u16
  - nodePool: centos7

build:
  ci:
    - docker info
    - docker ps -a
    - docker images
    - |
      [ $(docker version --format {{.Server.Version}}) == "18.03.1-ce" ]
